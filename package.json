{
  "name": "definition_check_vercel",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./src/index.html ./src/check.html --outdir=dist --minify && bun run copy-public",
    "copy-public": "bun scripts/copy-public.js",
    "dev": "bun scripts/dev.js",
    "preview": "bun run build && npx serve dist -l 3000",
    "start": "bun run dev"
  },
  "dependencies": {
    "pdfjs-dist": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
